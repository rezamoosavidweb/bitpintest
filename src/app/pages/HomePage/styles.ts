import { Grid, styled } from '@mui/material';

export const CurrencyCardContainer = styled(Grid)(({ theme }) => ({
    backgroundColor: '#ffffff',
    padding: '20px',
    borderRadius: '15px',
    boxShadow: '0 0 30px 0 #00000010',
    minHeight: '180px',
}));
